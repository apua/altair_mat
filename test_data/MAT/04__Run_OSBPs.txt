*** Settings ***
Resource    resource.txt
Force Tags  Run OSBPs
Test Template   With ${sut} Run OSBP: ${osbp}

*** Test Cases ***
Run OSBPs
    16.153.112.91   ProLiant HW - Boot Linux Service OS
    16.153.112.90   ProLiant HW - Boot Linux Service OS
    16.153.112.91   ProLiant HW - Boot Linux Service OS

*** Keyword ***
With ${sut} Run OSBP: ${osbp}
    Log to console  OSBP name: ${osbp}
    Log to console  SUT iLO: ${sut}
    ${job_uri}=     Run OSBP    ${osbp}     ${sut}
    Log to console  ${job_uri}
    ${status}=      wait job finish     ${job_uri}
    Should be equal  ${status}   ok

